server {
    listen 80;
    server_name noor.tenancyos.com;

    # Cloudflare Optimization
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # NOOR API & WebSockets
    location / {
        proxy_pass http://noor-backend:8000;
        proxy_http_version 1.1;
        
        # WebSocket Support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_read_timeout 86400;
    }

    # Custom Error Handling (JSON for Frontend)
    error_page 502 /502.html;
    location = /502.html {
        return 502 '{"error": "NOOR AI Backend is booting or offline"}';
        add_header Content-Type application/json;
    }
}

# 🔐 SSL BLOCK (Optional if using Cloudflare Flexible)
# UNCOMMENT AND FIX PATHS TO ACTIVATE
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name noor.tenancyos.com;
#
#     ssl_certificate /etc/nginx/ssl/origin.crt;
#     ssl_certificate_key /etc/nginx/ssl/origin.key;
#
#     location / {
#         proxy_pass http://noor-backend:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#     }
# }
